# 功能做完還不夠找得到：我把收藏清單放進 LINE Rich Menu

上一篇，我替 LINE Cafe Bot 加上了想去清單。

使用者可以從推薦卡收藏咖啡廳，之後輸入「我的想去清單」，再從收藏開啟地圖、安排時間或移除。

功能已經能用，但還有一個很現實的問題：

**幾天後，使用者還記得要輸入哪一句話嗎？**

如果一個功能只能靠文章或說明文件提醒，它就還沒有真正進入產品。於是今天的第二步，是把收藏清單放到使用者每天都看得到的 LINE Rich Menu。

完整程式碼與選單素材：
https://github.com/zonawang/line-cafe-wishlist

## 原本四格已經都有工作

原本的 Rich Menu 是四宮格：

```text
┌────────────┬────────────┐
│ 找附近咖啡 │  我的偏好   │
├────────────┼────────────┤
│ 設定偏好   │  使用說明   │
└────────────┴────────────┘
```

最簡單的做法，是拿掉其中一格換成收藏清單。

但這代表新增一個功能，卻讓原本的入口消失。最後我選擇保留上排兩個主要入口，把下排改成三等分：

```text
┌────────────┬────────────┐
│ 找附近咖啡 │  我的偏好   │
├────────┬────────┬────────┤
│設定偏好│收藏清單│使用說明│
└────────┴────────┴────────┘
```

「找附近咖啡」和「我的偏好」維持原本的大區塊；新增的收藏清單則放在下排中央，正好位於設定和說明之間。

這樣不需要增加第二頁，也不會讓使用者為了找收藏再切換選單。

![Zona Cafe Bot 五格 Rich Menu](https://raw.githubusercontent.com/zonawang/line-cafe-wishlist/main/assets/rich-menu-v3-wishlist.png)

## 圖片和點擊區域其實是兩件事

Rich Menu 看起來是一張圖片，但 LINE 真正判斷點擊位置時，使用的是另一份 JSON 設定。

這次圖片尺寸維持：

```text
2500 × 1686 px
```

上排仍然左右各半：

```text
左上：x 0～1249
右上：x 1250～2499
高度：843 px
```

下排 2500 px 無法完全除以 3，因此分成：

```text
833 px + 834 px + 833 px = 2500 px
```

中間多出來的一個像素放在收藏清單，讓五個點擊區域剛好覆蓋整張圖片，沒有空隙，也沒有互相重疊。

視覺上的卡片之間可以留白，但真正的點擊 bounds 仍然要完整相接。否則使用者看起來明明點在按鈕上，LINE 卻可能沒有反應。

## 收藏入口為什麼使用 Message Action？

Rich Menu 的收藏區塊被點擊後，會送出：

```text
我的想去清單
```

我沒有另外建立一個只給 Rich Menu 使用的特殊 API，也沒有新增另一套 Postback 格式。

原因很簡單：Bot 原本就能處理這句話。

Rich Menu 使用 Message Action 後，按鈕和手動輸入會走相同的 handler：

```text
使用者手動輸入 ─┐
                 ├→ wishlist text handler → 顯示清單
點 Rich Menu ────┘
```

這樣有幾個好處：

- 不需要維護兩條幾乎相同的程式路徑。
- 使用者在聊天畫面中看得到自己觸發的文字。
- 發生問題時，比隱藏的操作更容易理解與測試。
- 日後更換 Rich Menu，也不影響收藏功能本身。

Postback 很適合攜帶 session ID 或 item ID；但這裡只是開啟清單，一句既有指令就足夠了。

## 不是直接覆蓋正在使用的選單

Rich Menu 同時包含設定與圖片，而且正式 Bot 正在使用舊版。

如果先改掉舊選單，再慢慢上傳圖片，一旦中途失敗，使用者可能會看到沒有圖片、點擊區域錯位，甚至整個選單消失。

因此這次採用的是「建立新版，最後才切換」：

```text
讀取目前使用中的 Rich Menu
             ↓
建立五格 JSON 與新版圖片
             ↓
呼叫 LINE validation endpoint
             ↓
建立新的 Rich Menu
             ↓
上傳 PNG
             ↓
設為 default Rich Menu
```

在最後一步完成以前，使用者仍然看到原本的四格選單。只有 JSON 和圖片都準備好後，才將 default 指向新版。

舊 Rich Menu 也沒有立刻刪除。如果新版在手機上發現問題，仍然可以快速切回去，不必從頭重建。

## 圖片改版只動下半部

為了讓使用者一眼認出這仍然是原本的 Cafe Bot，我保留了上排兩張卡片的每一個像素，只重新設計下排。

新版下排使用三種延續原本風格的顏色：

- 設定偏好：陶土橘。
- 收藏清單：深咖啡綠。
- 使用說明：深棕色。

收藏清單使用書籤和星星圖示，文字則保持很直接：

```text
收藏清單
保存想去的咖啡館
```

這類介面圖片最怕文字被改錯、圖案和點擊區域對不起來。因此我用固定尺寸重新組合下排，而不是讓生成式模型自由重畫整張圖。

最終 PNG 是 2500 × 1686，檔案約 316 KB。圖片和 JSON 都一起放進 repo 的 `assets`，未來不需要從 LINE 後台猜測目前使用的是哪一版。

## 上線後，再從 LINE API 讀回來一次

API 回覆「成功」還不代表所有內容都正確。

切換完成後，我再次向 LINE 查詢目前的 default Rich Menu，確認：

- Default ID 已經是新版。
- 名稱是 `Zona Cafe Menu v3 Wishlist`。
- 點擊區域共有 5 個。
- 中間區域會送出「我的想去清單」。
- 從 LINE 下載回來的圖片，SHA-256 與 repo 中的 PNG 完全一致。

最後一項看起來有點嚴格，但很實用。它能確認上傳的不是舊檔、縮圖或錯誤版本，而是真正準備好的那張圖片。

設定檔與圖片提交後，GitHub Actions 也再次通過。

## Cloud Run 和 Rich Menu 是兩次不同的上線

這次也讓我更清楚看到，LINE Bot 的「介面」和「功能」不是部署在同一個地方。

想去清單的程式與 Firestore 邏輯部署在 Cloud Run；Rich Menu 則保存在 LINE 平台。只完成其中一邊，都會留下不完整的體驗：

```text
只有程式：功能存在，但使用者不一定找得到
只有選單：按鈕看得到，但後端不認得指令
兩邊完成：點下收藏清單，立刻看到自己的資料
```

所以我的上線順序是先部署 wishlist 功能、確認服務正常，再切換 Rich Menu。即使使用者在新版選單出現後立刻點擊，後端也已經準備好了。

## 一個入口，讓功能真的成為產品的一部分

把想去清單做完時，我原本以為今天的工作已經結束。

但 Rich Menu 改版提醒我：工程師知道某個指令存在，不代表使用者也知道。

現在使用者不必記住「我的想去清單」這句話。打開聊天室，就能看到收藏入口；而熟悉文字操作的人，仍然可以直接輸入原本的指令。

這次沒有新增更多 AI，也沒有改變推薦演算法。真正完成的是一段更容易被使用的路徑：

```text
推薦卡收藏 → Rich Menu 找回 → 從清單繼續行動
```

功能被看見、找得到、點得下去，而且後續有明確下一步，它才不只是藏在程式碼裡的一段能力。

## 本篇完整程式碼與 Rich Menu 素材

GitHub：
https://github.com/zonawang/line-cafe-wishlist

Rich Menu 設定與圖片位於：

```text
assets/rich-menu-v3-wishlist.json
assets/rich-menu-v3-wishlist.png
```

LINE Rich Menu 官方文件：
https://developers.line.biz/en/docs/messaging-api/using-rich-menus/
